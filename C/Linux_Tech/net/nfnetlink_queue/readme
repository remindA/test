2018.08.28
    1. ip报文的修改后必须要重新计算udp/tcp的校验和
    2. ip的校验和不必在应用层计算，内核会再次校验
    3. udp: 修改后的payload长度可以和原来payload长度不同
    4. tcp: 修改后的payload长度最好和原来的payload长度相同
        tcp的序列号和确认号与payload的长度有关
    5. 那么如果修改后的长度不同，那么如何修改确认号与序列号?

2018.08.29
    1. 确认号和序列号维护
